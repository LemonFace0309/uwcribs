query GetPosts($options: PostOptions) {
  posts(options: $options) {
    id
    type
    title
    description
    imageUrl
    availableBeds
    totalBeds
    baths
    amenities
    ppp
    fbLink
    genderRestriction
  }
}
